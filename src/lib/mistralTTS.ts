/**
 * [STUB] Ancienne intégration MistralTTS cassée — supprimée, remplacée par voxtralTTS.ts.
 * Ce stub maintient la signature pour voice.service.ts jusqu'à la migration B4.
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export async function synthesizeWithMistral(_text: string): Promise<Buffer | null> {
  return null;
}
